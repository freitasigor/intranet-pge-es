@using Orchard.ContentManagement
@using Orchard.ContentPicker.Fields
@using Orchard.Utility.Extensions;

@{
    var field = (ContentPickerField) Model.ContentField;
    string name = field.DisplayName;
    var contentItems = field.ContentItems;
    var contentManager = Model.ContentItem.ContentManager;
}


<div class="content-picker-field content-picker-field-@name.HtmlClassify()">
    @if(contentItems.Any()) {
        foreach (var contentItem in contentItems)
        {
            @*
            string stereotype;
            @contentItem.TypeDefinition.Settings.TryGetValue("Stereotype", out stereotype)
            @stereotype
            *@
            @Display(contentManager.BuildDisplay(contentItem, "SummaryContentPicker"))
            
        }
    }

</div>

